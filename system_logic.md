# AI Graphic 생성기 작동 구조 및 시스템 지침

## 1. 생성기 작동 구조 (Architecture)

본 애플리케이션은 **React(Vite)** 기반의 클라이언트 사이드 싱글 페이지 앱(SPA) 구조로 작동하며, **@google/genai** SDK를 활용하여 Gemini API와 통신합니다. 생성기는 사용자의 입력(이미지, 설정 옵션 등)을 조합하여, 모델이 이를 이해하고 이미지를 생성할 수 있도록 정교한 시스템 프롬프트를 자동으로 구축하여 API에 전달합니다.

### 데이터 처리 및 통신 흐름
1. **의류/상품 이미지 및 기준 모델 이미지 업로드**: 
   사용자가 등록한 이미지는 로컬에서 Base64 텍스트 형식으로 변환되어 일시적으로 메모리에 저장됩니다.
2. **배치(Batch) 기반 병렬 처리**:
   - `gemini-3-pro-image-preview` 등 최신 이미지 모델의 경우 동시 요청(Rate Limit)에 민감할 수 있습니다.
   - 따라서 전체 생성 수량(`count`)을 정해진 수량(기본 `3`장) 단위의 배치(Batch)로 나누어 병렬로 요청합니다.
   - 한 배치의 처리가 완료되면 약간의 대기 시간을 가져 API 할당량 초과(429 Too Many Requests 방지)를 예방합니다.
3. **API 결괏값 처리**:
   Base64 형태(`inlineData`)로 반환받은 이미지를 브라우저 상에 `data:image/png;base64,...` 형태로 렌더링하고, 다운로드 기능을 제공합니다.

---

## 2. 세팅된 시스템 프롬프트 (System Prompts)

사용자가 입력한 `기본 요청 사항`과 `설정 옵션(비율, 수량 등)`은 다음과 같이 구조화되어 강력한 시스템 지침과 함께 Gemini API에 전달됩니다. 아래는 자바스크립트 코드 내부에서 동적으로 조립되는 프롬프트의 뼈대입니다.

### [필수 기반 프롬프트]
```text
전문적인 패션 스튜디오 상품 상세 컷입니다. 
의류: 모델은 사진으로 업로드된 상품 이미지(누끼컷)를 위화감 없이 완벽하고 통일성 있게 착용하고 있습니다. 
[매우 중요 시스템 지침]: 업로드된 상품 이미지는 상품 그 자체(누끼컷)입니다. 이미지 생성 시 이 상품의 원본 형태, 로고, 색상, 질감, 핏을 절대로 왜곡하거나 임의로 변형하지 말고 원래의 형태 그대로 반영하여 모델에 입혀야 합니다.
[모델 포즈 및 에티튜드 필수 지침]: 모델이 단순히 멀뚱하게 서 있는 어색한 자세는 절대 피하세요. 룩북이나 하이엔드 패션 화보에 등장하는 프로페셔널한 모델처럼, 옷의 핏과 무드를 살릴 수 있는 자연스럽고 느낌(Vibe) 있는 자세를 취해야 합니다.
[톤앤매너 및 배경 일관성 지침]: 전체적인 화보의 조명, 배경, 감도, 채도, 톤앤매너는 하이엔드 패션 화보 수준의 프로페셔널한 스튜디오 촬영 결과를 유지하여 일관성 있게 구성해야 합니다. 단, 사용자가 작성한 [기본 요청 사항]에 배경, 조명, 분위기 등에 대한 구체적인 묘사가 있다면 그 요청을 무조건 제일 먼저 최우선으로 따르세요.
포즈: {동적으로 변하는 포즈 - 예: 주머니에 손을 넣고 서 있는 포즈}.
촬영 구도: {동적으로 변하는 촬영 구도}, {근거리/중거리/원거리} 촬영.
모델이 자연스럽고 비율이 좋게 보이도록 구성하세요.
```

### [화면 분할 및 구성 지침 (조건부 적용)]
**Case 1. 단일 컷 생성 시 (`한 장에 포함할 이미지 수 = 1` / Graphic 모드)**
```text
[최우선 절대 원칙 - 단일 컷]: 이 이미지 안에는 오직 단 1개의 컷, 단 1명의 피사체/장면만 있어야 합니다. 화면 분할, 콜라주, 한 화면에 여러 번 등장하는 것 등 2개 이상의 컷이 포함되는 것은 절대로 금지합니다. 사용자의 어떤 추가 요청이 있더라도 이 원칙을 무조건 최우선으로 지키세요.
```
**Case 2. 다중 컷 분할/콜라주 시 (`한 장에 포함할 이미지 수 > 1` / Graphic 모드)**
```text
[최우선 절대 원칙 - 다중 컷 분할]: 이 이미지 안에는 반드시 서로 다른 구도나 컷이 콜라주/분할 형태로 정확히 {N}개 포함되어야 합니다. 사용자의 어떤 추가 요청(기본 요청 사항)이 있더라도 이 숫자({N} 컷)를 엄격하게 지키세요.
```

**Case 3. 단일 장면 고정 (Concept 모드)**
```text
[최우선 절대 원칙 - 단일 컷]: 이 이미지 안에는 오직 단 1개의 컷, 단 1명의 피사체/장면만 있어야 합니다. 화면 분할, 콜라주, 한 화면에 여러 번 등장하는 것 등 2개 이상의 컷이 포함되는 것은 절대로 금지합니다. 사용자의 어떤 추가 요청이 있더라도 이 원칙을 무조건 최우선으로 지키세요.
```

### [시선, 자세, 시점의 변화 제어 (Graphic 모드)]
사용자가 0~10 (또는 1~10) 사이의 슬라이더로 조절한 수치량에 따라 프롬프트가 동적으로 변화하여 주입됩니다. 수치에 기반해 다음과 같이 적용됩니다.

*   **시선의 변화 정도**
    *   **0~3 (고정)**: 레퍼런스 이미지의 시선 방향이나 응시 방향을 엄격하게 유지.
    *   **4~6 (약한 변주)**: 시선 방향을 약간 비틀거나 자연스럽게 변주.
    *   **7~10 (파격적 변주)**: "카메라 응시", "다른 곳 바라보기", "아래쪽 바라보기" 등 무작위 파격 변주.

*   **자세의 변화 정도**
    *   **0~3 (고정)**: 원본의 체형 윤곽, 몸통 방향, 팔다리 자세를 그대로 유지.
    *   **4~6 (약한 변주)**: 팔/다리 위치 및 몸통의 방향을 살짝 바꾸어 자연스러운 룩북 연출.
    *   **7~10 (파격적 변주)**: 무작위로 앉은 자세, 스포티한 자세, 옆모습 등 기존과 완전히 다른 포즈 주입.

*   **시점의 변화 정도**
    *   **1~3 (고정)**: 기존 배경/피사체와 같은 앵글 및 스팟 유지.
    *   **4~6 (약한 변주)**: 동일 공간 내에서 구도나 각도를 약간 올리거나 내려 변경.
    *   **7~10 (파격적 변주)**: 동일 공간 내에 있더라도 "하이 앵글", "극단적 로우 앵글", "오버더숄더" 등 무작위 카메라 뷰로 촬영.

### [컨셉 생성 지침 (Concept 모드)]
Concept 모드에서는 레퍼런스와 완벽히 동일한 장소(특정 스팟)에서 카메라가 살짝 다른 곳을 바라보고 찍은 듯한 1장의 사진을 생성하도록 지시합니다. 레퍼런스의 톤앤매너, 사진 스타일, 보정 느낌, 조명 등은 완벽하게 동일해야 합니다.
```

### [사용자 맞춤 요청 지침]
사용자가 `기본 요청 사항` 입력 폼에 내용을 작성하면 다음의 텍스트가 덧붙여집니다.
```text
[기본 요청 사항 - 이 지침을 반드시 최우선으로 따를 것]: {사용자가 입력한 커스텀 프롬프트 텍스트}
```

### [기준 인물 모델 이미지 반영 (조건부 적용)]
추가적인 인물 사진 래퍼런스가 업로드되었다면 아래의 지침이 자동으로 따라붙어 얼굴/체형의 연속성을 유지시킵니다.
```text
주의: 최대 5개의 인물 예시 이미지가 제공되었습니다. 새롭게 만들어지는 모델의 체형과 외모(얼굴, 머리스타일 등)는 오직 이 예시 이미지들의 모델을 최우선으로 반영하여 통일성 있게 생성하세요.
```

### [컨셉 배경 이미지 반영 (조건부 적용)]
컨셉 배경 이미지가 업로드되었다면 아래의 지침이 자동으로 따라붙어 업로드된 이미지의 무드와 톤앤매너를 유지시킵니다.
```text
[배경 및 감도 필수 지침]: 추가로 제공된 컨셉 배경 이미지들이 있습니다. 새롭게 만들어지는 화보의 배경, 채도, 감도, 조명 등 전체적인 무드와 톤앤매너는 반드시 이 배경 이미지들의 느낌을 최우선으로 반영하여 생성하세요.
```

---

## 3. 기능 한계 및 현재 적용된 화질 세팅

현재 서비스는 **Gemini Imagen / Gemini AI Native Image Generation**을 구동하도록 세팅되어 있습니다. 
API 설정 파라미터를 통해 다음과 같은 스펙으로 출력되도록 설정되어 있습니다:

- **생성 해상도(Image Size)**: `1K`, `2K`, `4K` 출력 해상도를 사용자가 선택할 수 있도록 구성되어 있습니다 (API 내부 속성: `imageSize` 설정 사용). 
    - `1K`: 약 1024x1024 해상도 (약 100만 화소). 720p(HD, 약 90만 화소)보다 조금 더 선명하며, 웹/모바일 최적화에 가장 적절합니다.
    - `2K`: 약 2048x2048 해상도 (약 400만 화소). 1080p(FHD, 약 200만 화소)를 능가하는 해상도입니다.
    - `4K`: 약 4096x4096 해상도 (약 1600만 화소). 압도적인 초고해상도로 부분 크롭 등 디테일 편집에 유리합니다.
- **이미지 비율(Aspect Ratio)**: 사용자가 지정한 값을 바탕으로 `1:1`, `4:3`, `3:4`, `16:9`, `9:16` 비율 중 하나를 생성합니다.
- **이미지 퀄리티/품질**: 현재 호출되는 모델(`gemini-3-pro-image-preview`)은 제공된 이미지를 참조(레퍼런스)하여 합성을 수행하는 방식을 취하며, 원본 이미지의 퀄리티와 프롬프트 해석에 따라 상업용 수준 화질을 타겟하여 이미지를 반환합니다.
